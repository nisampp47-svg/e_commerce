name: e_commerce
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'


dependencies:
  flutter:
    sdk: flutter
  supabase_flutter:
  provider:
  sqflite:
  google_fonts:
  shimmer:
  cached_network_image:
  shared_preferences:
  url_launcher:
  razorpay_flutter:
  cupertino_icons:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
